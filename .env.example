# No environment variables required. The contact form is wired to Formspree
# (endpoint hardcoded in src/pages/Contact.jsx — it's a public form ID, not a
# secret, so it doesn't need to be an env var).
